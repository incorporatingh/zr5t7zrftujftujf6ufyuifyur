name: Run Automation

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

env:
  DISPLAY_NUM: "99"
  DISPLAY: ":99"
  PYTHONUNBUFFERED: "1"

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      # ── 1. System packages ────────────────────────────
      - name: Block Snap Firefox + add Mozilla repo
        run: |
          sudo tee /etc/apt/preferences.d/block-firefox-snap > /dev/null << 'PIN'
          Package: firefox*
          Pin: release o=Ubuntu
          Pin-Priority: -1
          PIN

          sudo tee /etc/apt/preferences.d/mozilla > /dev/null << 'PIN'
          Package: firefox*
          Pin: origin packages.mozilla.org
          Pin-Priority: 1001
          PIN

          sudo install -d -m 0755 /etc/apt/keyrings
          wget -qO- https://packages.mozilla.org/apt/repo-signing-key.gpg \
            | sudo tee /etc/apt/keyrings/packages.mozilla.org.asc > /dev/null

          echo "deb [signed-by=/etc/apt/keyrings/packages.mozilla.org.asc] \
            https://packages.mozilla.org/apt mozilla main" \
            | sudo tee /etc/apt/sources.list.d/mozilla.list > /dev/null

      - name: Install system packages
        run: |
          sudo apt-get update
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
            python3-pip python3-tk python3-dev python3-venv \
            xvfb x11-xserver-utils x11-utils xauth dbus-x11 \
            xfce4 xfce4-goodies openbox \
            wmctrl xclip scrot xdotool \
            x11vnc novnc python3-websockify \
            firefox \
            procps

      # ── 2. Camoufox cache ─────────────────────────────
      - name: Restore camoufox binary cache
        id: cache-camoufox
        uses: actions/cache@v4
        with:
          path: |
            ~/.camoufox
            ~/.cache/camoufox
          key: camoufox-${{ runner.os }}-${{ hashFiles('_private/scripts/requirements-refs.txt') }}

      # ── 3. Private scripts ────────────────────────────
      - name: Fetch private scripts
        uses: actions/checkout@v4
        with:
          repository: incorporatingh/turing
          token: ${{ secrets.PRIVATE_REPO_PAT }}
          path: _private

      # ── 4. Python deps ────────────────────────────────
      - name: Cache pip packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: pip-${{ runner.os }}-${{ hashFiles('_private/scripts/requirements.txt') }}
          restore-keys: pip-${{ runner.os }}-

      - name: Install Python dependencies
        run: pip3 install --break-system-packages -r _private/scripts/requirements.txt

      - name: Fetch camoufox browser binary
        if: steps.cache-camoufox.outputs.cache-hit != 'true'
        run: python3 -m camoufox fetch

      # ── 5. Display :99 — for Reddit script (XFCE desktop) ──
      - name: Start display :99 (Reddit desktop)
        run: |
          touch ~/.Xauthority
          rm -f /tmp/.X99-lock /tmp/.X11-unix/X99

          Xvfb :99 -screen 0 1920x1080x24 +extension RANDR &

          for i in $(seq 1 20); do
            if [ -S /tmp/.X11-unix/X99 ]; then
              echo "Xvfb :99 ready"
              break
            fi
            [ "$i" -eq 20 ] && { echo "Xvfb :99 failed"; exit 1; }
            sleep 1
          done
          sleep 2

          dbus-launch --exit-with-session startxfce4 &

          for i in $(seq 1 30); do
            if pgrep -x xfwm4 &>/dev/null; then
              echo "Window manager ready on :99"
              break
            fi
            [ "$i" -eq 30 ] && echo "WARNING: xfwm4 not detected"
            sleep 1
          done

      # ── 6. Display :1 — for Camoufox temp-mail bot ────
      #    Separate display so camoufox's headful browser
      #    doesn't interfere with the Reddit automation.
      - name: Start display :1 (Camoufox temp-mail)
        run: |
          rm -f /tmp/.X1-lock /tmp/.X11-unix/X1

          Xvfb :1 -screen 0 1280x720x24 &

          for i in $(seq 1 20); do
            if [ -S /tmp/.X11-unix/X1 ]; then
              echo "Xvfb :1 ready"
              break
            fi
            [ "$i" -eq 20 ] && { echo "Xvfb :1 failed"; exit 1; }
            sleep 1
          done

          echo "Display :1 ready for camoufox"

      # ── 7. VNC + Cloudflare tunnel ────────────────────
      - name: Start VNC and debug tunnel
        run: |
          # VNC on :99 (the Reddit desktop)
          x11vnc -display :99 -forever -shared -nopw \
            -rfbport 5900 > /tmp/x11vnc.log 2>&1 &

          websockify --web=/usr/share/novnc/ 6080 127.0.0.1:5900 \
            > /tmp/novnc.log 2>&1 &

          sleep 2

          # Cloudflare quick tunnel
          wget -qO /tmp/cloudflared \
            https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
          chmod +x /tmp/cloudflared

          nohup /tmp/cloudflared tunnel --url http://localhost:6080 \
            > /tmp/cf-tunnel.log 2>&1 &

          TUNNEL_URL=""
          for i in $(seq 1 30); do
            TUNNEL_URL=$(grep -oP 'https://[a-z0-9-]+\.trycloudflare\.com' \
              /tmp/cf-tunnel.log 2>/dev/null | head -1)
            if [ -n "$TUNNEL_URL" ]; then
              break
            fi
            sleep 2
          done

          echo ""
          echo "╔══════════════════════════════════════════════════════════╗"
          echo "║  LIVE DESKTOP (:99 — Reddit Firefox)                    ║"
          if [ -n "$TUNNEL_URL" ]; then
            echo "║  ${TUNNEL_URL}/vnc.html"
          else
            echo "║  FAILED: cloudflared tunnel did not start               ║"
          fi
          echo "║                                                         ║"
          echo "║  Note: camoufox runs on :1 (not visible in VNC)         ║"
          echo "╚══════════════════════════════════════════════════════════╝"
          echo ""

      # ── 8. Verify ─────────────────────────────────────
      - name: Verify environment
        run: |
          echo "=== Display :99 ==="
          xdpyinfo -display :99 2>&1 | head -3
          echo "=== Display :1 ==="
          xdpyinfo -display :1 2>&1 | head -3
          echo "=== Firefox ==="
          firefox --version
          echo "=== Camoufox ==="
          python3 -c "from camoufox.sync_api import Camoufox; print('import OK')" 2>&1
          echo "=== PyAutoGUI ==="
          python3 -c "import pyautogui; print('import OK')" 2>&1
          echo "=== Processes ==="
          ps aux | grep -E 'Xvfb|xfwm4|x11vnc|cloudflared' | grep -v grep

      # ── 9. Run scripts ────────────────────────────────
      - name: Run automation
        working-directory: _private/scripts
        run: |
          # tempmail_bot.py sets DISPLAY=:1 internally (camoufox on :1)
          python3 tempmail_bot.py &
          TEMPMAIL_PID=$!

          # reddit_script_2.py uses DISPLAY=:99 (Firefox on :99)
          python3 reddit_script_2.py || true

          kill $TEMPMAIL_PID 2>/dev/null || true
          wait $TEMPMAIL_PID 2>/dev/null || true

          echo "=== shared_data.json ==="
          cat shared_data.json 2>/dev/null || echo "(not created)"

          # Determine success
          if python3 -c "
          import json, sys
          d = json.load(open('shared_data.json'))
          if d.get('error') or not d.get('otp'): sys.exit(1)
          " 2>/dev/null; then
            echo "Automation completed successfully"
          else
            echo "Automation failed — see logs above"
            exit 1
          fi

      # ── 10. Debug artifacts ───────────────────────────
      - name: Capture screenshot
        if: always()
        run: |
          DISPLAY=:99 scrot /tmp/display99.png 2>/dev/null || true
          DISPLAY=:1 scrot /tmp/display1.png 2>/dev/null || true

      - name: Upload screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: run-screenshots
          path: /tmp/display*.png
          if-no-files-found: ignore
          retention-days: 7
